#include <stdio.h>
#include <conio.h>
void main()
{
int i, len, n = 0;
char str[10];
clrscr();
printf("Nishu Rana--2001330109016\n\nEnter a string:");
gets(str);
   len = strlen(str);
   while(n<len)
   {
   if(isdigit(str[i]))
   {
   }
   else
   {
   }
   n++;
   printf("it is not a constant");
   if(n==len)
   printf("\nit is not a constant:");
   getch();
   }
